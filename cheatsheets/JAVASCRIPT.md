### Arrays

Define array:

```
> var dict = [];
```

Append dictionary to array:

```
> dict.push({
    key:   "name",
    value: "ruan"
});
```

Print the array:

```
> console.log(dict);
[ { key: 'name', value: 'ruan' } ]
```
